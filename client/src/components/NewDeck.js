import { useState, useEffect } from 'react';

const NewDeck = () => { // somehow store the new deck data in an upper state so that if the page is refreshed, then the data will not be lost. => redux?

}

export default NewDeck;